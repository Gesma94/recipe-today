model User {
  id           Int      @id @default(autoincrement())
  firebase_uid String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  displayName  String
  chat         Chat[]
}

